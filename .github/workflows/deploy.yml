name: Node.js CI

on:
  push:
    branches: ["master"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: docker build . -t ${{secrets.DOCKERHUB_USER}}/github-actions:latest
      - run: echo "${{ secrets.DOCKERHUB_TOKEN}}" | docker login -u ${{secrets.DOCKERHUB_USER}} --password-stdin
      - run: docker  push ${{secrets.DOCKERHUB_USER}}/github-actions:latest
